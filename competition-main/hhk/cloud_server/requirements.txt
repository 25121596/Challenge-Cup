# Cloud Edge Server — Python dependencies
#
# Install:
#   pip install -r requirements.txt
#
# Core:
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
httpx>=0.27.0

# For the optional PPO scheduler (only needed when CLOUD_USE_PPO_SCHEDULER=true):
# stable-baselines3>=2.0.0
# torch>=2.0.0

# For feature upload decoding (optional, used at runtime if available):
numpy>=1.24.0

# Testing:
pytest>=8.0.0
pytest-asyncio>=0.24.0
